/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export'
}

module.exports = nextConfig
module.exports = {
  // ... other configurations ...
  images: {
    unoptimized: true,
  },
};